# .env.example - example environment variables for dbestech_ecomly_server
# Copy this file to `.env` and fill real values. Do NOT commit real secrets.

## Server
HOST=localhost
PORT=3000

## MongoDB
# Choose environment: local or cloud
# Example: local  -> MONGO_ENV=local
# Example: cloud  -> MONGO_ENV=cloud
MONGO_ENV=local

# Local MongoDB (used when MONGO_ENV=local)
MONGO_LOCAL_URI=mongodb://localhost:27017/ecomly_local

# MongoDB Atlas (cloud) - replace placeholders if using Atlas
# MONGO_DB_USERNAME=your_db_user
# MONGO_DB_PASSWORD=your_db_password
# MONGO_CLOUD_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/ecomly?retryWrites=true&w=majority

## JSON Web Tokens
JWT_ACCESS_TOKEN_SECRET=replace_with_access_secret
JWT_REFRESH_TOKEN_SECRET=replace_with_refresh_secret

## Proxy (optional)
# HTTP_PROXY=http://proxy-host:port
# HTTPS_PROXY=http://proxy-host:port
# NO_PROXY=localhost,127.0.0.1

## API
API_URL=/api/v1

## Email (for development/testing)
EMAIL_NAME=Your Name
EMAIL_USERNAME=your.email@ethereal.email
EMAIL_PASSWORD=your_email_password
